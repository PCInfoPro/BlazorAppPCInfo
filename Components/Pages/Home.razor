@page "/"
@inject IStringLocalizer<BlazorAppPCInfo.Home> Localizer
@inject IStringLocalizer<Resources> LocResources

<PageTitle>Home</PageTitle>

<h1>@Localizer["greeting"]</h1>

<div class="card mb-4">
    <div class="card-body">
        <p class="card-text">
            <code>
                @("Modal.Show<Confirm>(\"Welcome to Blazored Modal\", options);")
            </code>
        </p>
    </div>
</div>

<button @onclick="ShowModal" class="btn btn-primary">Show Modal</button>

@code {
    [CascadingParameter] public IModalService Modal { get; set; } = default!;

    void ShowModal() 
        => Modal.Show<Confirm>("Welcome to Blazored Modal");
}
